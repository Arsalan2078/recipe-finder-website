import { Link } from "react-router";
import css from "./index.module.scss";

export default function RecipeDetails() {
    return (
        <div>
            <div><Link to="/">Recipes</Link> / RecipeDetails</div>
        </div>
    );
}
